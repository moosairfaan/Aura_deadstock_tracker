# Fashion relevance API — app/main.py (FastAPI + embeddings)

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0,<3

# Embeddings & numerics
sentence-transformers>=3.0.0
torch>=2.0.0
numpy>=1.26.0
pandas>=2.0.0

# Outbound HTTP (Serper, trending keyword URLs, etc.)
httpx>=0.27.0
